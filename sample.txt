I want implement the React code for web templates. That means we get the data from AEM in the Json format. Each web template will have template type.  Based on the template type i've to render the data in react code. The web template will have some news or about some new building facility, article, etc., there're 1000 of articles. Basically the AEM response for Web template looks like below. Each template type will have a image, header text and follwed by description and then body and realted articles . The body can  be split into multiple bodies. example boby 1, body 2 , body n. Each body will have a type. If the type is text that means the content will be in html data in string format. next type can be group of images to disaply. next body type can be excerpts with data, next body type can be table with data static data source and next type can be test etc., and related articles typically will have image and description and link to same article type read.
